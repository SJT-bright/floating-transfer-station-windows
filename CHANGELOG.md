# 更新记录

本项目从 1.0.0 开始公开维护。版本号遵循 `主版本.次版本.修订号`。

## 未发布

## 1.5.1

- 修复调节透明度时从标题栏移入滑块会触发面板收回的问题。

## 1.5.0

- 新增窗口透明度 DIY 调节：标题栏 `◐` 滑块支持 35%—100% 实时预览。
- 透明度自动保存到本地设置，重启应用后保持；恢复默认设置会同时恢复默认透明度。

## 1.4.0

- 独立 Windows 发布：自带运行环境的 x64 安装包和免安装 ZIP。
- 自动剪贴板收集固定进入待分类；内置人物资产、场景、提示词和待分类。
- 分类栏增加加号，自定义类别及名称持久保存，滚动查看更多类别。
- 图片跨类别拖动复制独立文件，目标蓝色反馈，保留源条目。
- 半透明窗口，保留虚拟化列表、异步图片处理与展开动效。

## 1.1.2-mac

- 恢复展开动效：内容在 0.24 秒内轻柔滑入并淡入，窗口尺寸直接到位，不再逐帧改变文字排版宽度。
- 系统开启“减少动态效果”时只做短淡入；收起或开始拖动时取消未结束动效，避免内容残留偏移。
- 保留长文预览、完整文字复制和待分类自动收集规则。

## 1.1.1-mac

- 修复展开时卡在半宽窗口的问题：由窗口统一控制尺寸，不再让内容布局反向修改窗口边界。
- 展开和收起直接切换到最终尺寸，避免长文本在变宽动画中逐帧重新排版。
- 文字卡片采用最多 600 字、12 行预览；点击“查看全文”使用原生可滚动阅读窗口。保存、复制和拖出仍保留完整文字。
- 修复窗口样式切换后系统标题栏按钮意外露出。

## 1.1.0-mac

- 自动采集和手动读取剪贴板始终存入“待分类”，切换分类、鼠标离开面板均不改变接收位置；已有内容不迁移。
- 右上角新增“＋”，支持创建、改名和保存自定义类别；类别较多时可滚动查看。
- 自定义类别支持图片拖入复制和卡片菜单移动，保留原有蓝色拖放提示。
- 修复固定四分类的数据整理逻辑，防止新类别中的内容在保存或重启时丢失。

## 1.0.6-mac

- 修复分类变蓝但松开后复制失败：直接读取原生拖拽板中的图片地址，避免 SwiftUI 提供器无法加载文件地址。
- 已通过真实鼠标拖放验证：目标分类新增一张独立副本，原分类内容和数量保留。

### macOS 新增

- 图片可拖到右侧其他分类进行复制；悬停目标显示蓝色待放置状态，原分类保留原图。

## 1.3.0

### 新增

- 支持用 `Delete` 键删除当前选择；`Backspace` 保持兼容，分类名称编辑框仍保留原生文字删除语义。

### 可靠性

- 面板收起后，`Delete` 与 `Backspace` 不会删除不可见的保留选择。

## 1.2.0

### 新增

- 支持用 `Esc` 取消当前分类全部选择，分类名称编辑框仍保留取消改名语义。

### 可靠性

- 单卡或批量置顶正在保存时，较晚发生的 `Esc` 取消选择不会再被旧选择快照覆盖。

## 1.1.0

### 新增

- 支持通过顶部图钉或 `Ctrl + P` 对当前选择执行批量置顶与批量取消置顶；混合选择会统一置顶，全已置顶选择会统一取消置顶。
- 支持用 `Ctrl + A` 选择当前分类全部内容，分类名称编辑框仍保留文字全选语义。

### 可靠性

- 批量改变置顶状态只保存一次，并在保存失败时原子恢复每项状态、精确顺序、选择集合和滚动位置。

## 1.0.0

首次 GitHub 公开版本。

### 已有能力

- 自动收集剪贴板中的常见图片和非空文字。
- 四分类整理、分类改名、置顶、多选、批量移动、排序和删除。
- 从外部软件把通用图片或文字拖入指定分类。
- 把单项文字/图片或纯图片批次拖到其他软件。
- 贴边置顶、自动展开收起、滚动位置记忆和本地持久化。
- 可选择程序与数据目录，支持更新迁移、开机自启和完整卸载。

### 1.0 质量整理

- 按职责拆分主窗口实现，减少事件、拖放、删除和动画逻辑的耦合。
- 集中界面颜色、间距、圆角和操作列宽度，减少不同区域的视觉割裂。
- 补强动画中断、系统关闭动画、滚动恢复、删除回滚和拖放失败路径。
- 自动质量门达到 350/350，格式验证和严格 Release 构建通过。

### 已知限制

- 批量置顶与批量取消置顶计划在后续版本设计。
- B-005 与 B-006 仍是自动环境未稳定复现的低优先级现场观察。
